# Roblox Global Tracker
This project fetches global names, datatypes, and library names from the official Roblox Creator Docs and generates a combined list of them.
The results are saved into output.json, which is updated using GitHub Actions.
